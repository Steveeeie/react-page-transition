import styled from 'styled-components';

const PageTransitionGroup = styled.div`
  position: relative;
  width: 100%;
  height: 100%;
  perspective: 1200px;
  overflow: hidden;
`;

export { PageTransitionGroup };
